import React from 'react';
// import styled from 'styled-components';

const ItemPrice = (props) => {
    return(
        <div>
            <h4>cena: {props.price}</h4>
        </div>
    );
};
export default ItemPrice